read string from serial
extract first comma. this is the command. We only have one, which is "display image"
The rest of the string is the image, base64 encoded
Display it



need a way to put the image over the wire. It seems I don't have base64 decoding.
I could use one char for each pixel?
I need to extract them to array of 256. However, I can't just write them over the wire because of newline being a char.
Maybe json?

https://github.com/nopnop2002/esp-idf-json/blob/master/json-basic/main/main.c


loop:
read message
parse json
get "command" string field
get "paramters" string field
based on "command", call the right method, passing the parameters object.
delete json
